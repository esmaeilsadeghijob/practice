server:
  port: 6060

spring:
  application:
    name: practice
  datasource:
    url: jdbc:postgresql://localhost:5432/practice
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create